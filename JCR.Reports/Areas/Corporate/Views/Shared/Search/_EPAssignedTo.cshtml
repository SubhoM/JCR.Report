@model IEnumerable<JCR.Reports.Areas.Corporate.ViewModels.AssignedUser>
<div id="multisiteEPAssignedTo">
    <h6><b>Assigned To</b></h6>
    <div class="margintop">

        @(Html.Kendo().MultiSelect()
          .Name("EPAssignedTo")
                  .DataTextField("FullName")
                  .DataValueField("UserID")
          .Placeholder("Select Assigend To")
          .Filter(FilterType.Contains)
          .HighlightFirst(true)
          .BindTo(Model)
        .AutoClose(false)
        .Events(e =>
                    {
                        e.Select("onEPAssignedToSelect");
                    })
                  .Value("-1")
        )
    </div>
</div>

<script>
    $(document).ready(function () {
        addarrowtomultiselect("EPAssignedTo");
     });

</script>


















